spring:
  cloud:
    gateway:
      httpclient:
        connect-timeout: 1000
        response-timeout: 1s
gateway:
  a-url: http://localhost:${wiremock.server.port}
  b-url: http://localhost:${wiremock.server.port}
